# Secrets are loaded from .env file — never commit actual values
secrets:
  GEMINI_API_KEY:
    description: Google Gemini API key for LLM sentiment analysis
  PRIVATE_KEY:
    description: Deployer private key for signing transactions
